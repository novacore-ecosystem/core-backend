namespace NovaCore.User.Persistence.Contexts.Users.Repositories;

// Leave empty for now - nothing beyond generic CRUD is needed. Reserved for future scaling
// (e.g. a bulk lookup keyed by something other than Id).
public interface IUserRepository
{
}
