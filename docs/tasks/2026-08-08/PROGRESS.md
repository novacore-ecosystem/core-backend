# 2026-08-08 — Progress

- [x] Task 1 — Promotion Service Phase 3.1 Entity Configuration + Domain Correction (`Task1_promotion-service-phase3.1-entity-configuration-domain-correction.md`) — done: first Phase 3 (Persistence) prompt. Corrected all 10 Translation entities' identity to an explicit `{Parent}Id` + `LanguageCode` composite key (was a reused parent `Id`, a real defect), declared `: byte` on all 32 enums, ran a full bidirectional navigation-completeness sweep across all 103 entities. Wrote `IEntityTypeConfiguration<T>` for all 103 entities, wired `PromotionDbContext`'s 103 DbSets, froze the entity-configuration conventions doc. `Promotion.Domain` and `Promotion.Persistence` both build clean, 0 errors. Phase 3 remains in progress — no repository/persistence service/CQRS/migration yet.
- [x] Task 2 — Promotion Service Phase 3.2 Persistence Infrastructure Skeleton (`Task2_promotion-service-phase3.2-persistence-infrastructure-skeleton.md`) — done: second Phase 3 prompt. Computed the full 103-entity ownership graph, confirmed with the architect a roots-only repository granularity (11 true Aggregate Roots, not the ~76-entity mechanical alternative). Built 22 empty Application Read/Write interfaces + 44 Persistence files (repository/Read/Write per root), rewired DI (`AddRepositories` + full 103-entity `AddAuditHierarchy`), reused existing UnitOfWork/DbContext/tenant/audit infrastructure verbatim. Zero speculative methods added. `Promotion.API` builds clean end-to-end, 0 errors. Phase 3 remains in progress — no CQRS/migration yet.
- [x] Task 3 — Promotion Service Phase 3.3 Persistence Skeleton Implementation (`Task3_promotion-service-phase3.3-persistence-skeleton-implementation.md`) — done: third Phase 3 prompt. Re-read `docs/conventions/persistence-coding-conventions.md` against Phase 3.2's output and found one real deviation: all 11 Read Services had copied `Payment.Persistence`'s literal shape (injecting the repository) instead of the documented convention (inject `PromotionDbContext` directly, stay independent of the repository) — Payment predates the finalized convention. Fixed all 11 constructors; everything else already matched. `Promotion.API` builds clean, 0 errors. Phase 3 remains in progress.
- [x] Task 4 — Promotion Service Phase 3.4 Elasticsearch + Fuzzy Search Integration (`Task4_promotion-service-phase3.4-elasticsearch-fuzzy-search-integration.md`) — done: fourth Phase 3 prompt, absorbing the roadmap's originally separate Phase 4. Built public Coupon search infrastructure (document/mapping/indexer/repository/DI/startup wiring) cloning ProductSearch's exact architecture. Found and reconciled two real gaps with the architect: neither ProductSearch nor UserSearch actually has fuzzy search despite docs claiming otherwise (added standard ES `.Fuzziness("AUTO")` - first real fuzzy search in the platform); neither carries a tenant field despite their source entities being tenant-scoped (added a mandatory `TenantId` filter for Coupon, leaving Product/User's own gap unfixed, out of scope). No projection builder/sync events/endpoint built - explicitly Phase 5 (CQRS) work. `Promotion.API` builds clean, 0 errors. Phase 3 remains in progress.
- [x] Task 5 — Promotion Service Phase 3.5 Persistence Finalization (`Task5_promotion-service-phase3.5-persistence-finalization.md`) — done: fifth and final Phase 3 prompt. **Phase 3 (Persistence) now fully closed.** Verification-only, no large audit - confirmed DI/UnitOfWork/DbContext/Search wiring already complete and project references still minimal (no EF/Elasticsearch/Postgres leaking into Domain/Application). Nothing was genuinely missing, so no new source code was added. Updated the migration TODO to defer `dotnet ef migrations add InitialCreate` to an explicit future prompt, added a concise Persistence close-out doc section. `Promotion.API` builds clean, 0 errors, unchanged from Phase 3.4's baseline. Next: Phase 4 - Application/CQRS/API Skeleton.
- [x] Task 6 — Promotion Service Phase 4.1 Application/CQRS/Minimal API Skeleton (`Task6_promotion-service-phase4.1-application-cqrs-minimal-api-skeleton.md`) — done: first Phase 4 prompt. Built one representative Coupon Command (`CreateCoupon`) and Query (`GetCoupon`) flow end-to-end (Minimal API -> Request -> ISender -> Handler -> Persistence Service, DI-resolved), both handler bodies stubbed with `NotImplementedException` behind a `// TODO:` since no method exists yet on `ICouponReadService`/`ICouponWriteService` (adding one was explicitly forbidden). Found and reconciled two real gaps: no Request->Command Mapster precedent exists anywhere in the platform (only Entity->Response, which needs a real fetched entity this skeleton can't produce) - skipped Mapster entirely, matching Payment's own Create/Get pattern; no `Permissions.Promotion.*` constants exist - used generic `.RequireAuthorization()` instead of inventing one. Also corrected the Promotion progress tracker's own "X/7" arithmetic (it's a current-phase position indicator, not a completed count). `Promotion.API` builds clean, 0 errors (4 new expected unused-parameter warnings). Phase 4 remains in progress.
- [x] Task 7 — Promotion Service Phase 4.2 Coupon Management Foundation (`Task7_promotion-service-phase4.2-coupon-management-foundation.md`) — done: second Phase 4 prompt, first real Promotion feature. Implemented Create/Get/Update/Disable/List/Translate for Coupon, all real (no stubs) - extended `ICouponReadService`/`ICouponWriteService` with exactly the methods each operation needs, `CreateCouponHandler` constructs `Coupon.Create(...)` itself (matching Product's pattern), `UpdateCouponHandler` invokes 4 Domain methods in one transaction, `DisableCouponHandler` calls `Coupon.Disable()` (never a physical delete - no `Delete()` method exists), `TranslateCouponHandler` is one upsert command, `ListCouponsHandler` uses plain EF `PaginatedResult<T>` (not Elasticsearch - that stays Phase 3.4's separate concern). Added a Coupon Mapster config, resolving Phase 4.1's "necessarily unreachable" Entity->Response gap now that real persistence calls exist. Deliberately skipped FK-existence checks, code-uniqueness validation, and status-transition endpoints beyond Disable - out of this feature's scope. 6 Minimal API endpoints. `Promotion.API` builds clean, 0 errors, 24 warnings (4 stub warnings gone, 1 pre-existing unrelated obsolete-API warning left untouched). Phase 4 remains in progress.
